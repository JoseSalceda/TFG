@startuml ActividadSync

title Actividad - Sincronizacion de costes

start

:Administrador solicita sincronizacion;
:Validar usuario y organizacion;
:Recuperar credenciales cloud;

if (Credenciales disponibles?) then (no)
  :Finalizar sin sincronizar proveedor;
  stop
else (si)
endif

:Descifrar credenciales en memoria;

if (Credenciales validas?) then (no)
  :Registrar incidencia de sincronizacion;
  stop
else (si)
endif

:Consultar AWS Cost Explorer;

if (Respuesta correcta?) then (no)
  :Guardar resultado con error visible;
  stop
else (si)
endif

:Normalizar registros de coste Bedrock;

while (quedan registros?) is (si)
  :Asociar recurso con agente si existe senal fiable;
  :Persistir con update-or-insert;
endwhile (no)

:Registrar auditoria;
:Devolver resumen de sincronizacion;

stop

@enduml
